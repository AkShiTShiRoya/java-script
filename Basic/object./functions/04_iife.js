// Immediately Invoked Function Expression (IIFE)

(function  chai(){
    // named IIFE
    console.log(`Databasse connected `);
    
})();

((name) =>{
    console.log(`Database esthablished${name}`);
    
})("akshit ")